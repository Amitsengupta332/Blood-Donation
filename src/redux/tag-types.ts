export enum tagTypes {
  admin = "admin",
  user = "user",
  request = "request",
  post = "post",
}

export const tagTypesList = [
  tagTypes.admin,
  tagTypes.user,
  tagTypes.request,
  tagTypes.post,
];
