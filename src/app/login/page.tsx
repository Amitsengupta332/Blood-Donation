import { Container } from '@mui/material';
import React from 'react';

const LoginPage = () => {
    return (
        <Container>
            <h1>Login here...</h1>
        </Container>
    );
};

export default LoginPage;